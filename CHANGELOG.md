# CHANGELOG / 実装経緯・教訓の記録

このファイルはCLAUDE.mdとは違い**自動では読み込まれない**。過去に何が起きたか・なぜ今の設計になっているかを調べたい時にだけ、必要な箇所を検索・参照すること。CLAUDE.mdは「今の状態」だけを載せる方針にしたため、2026-09-18時点までの詳細な実装履歴・バグ調査・トラブルシュート記録をこちらに移した(内容は削除せずそのまま移動)。

## テスト関連の教訓

**Windowsでのハマりどころ**: テストスクリプト内で`spawn(cmd, { shell: true })`して立てた静的サーバーは、`server.kill()`はおろか`taskkill /pid <pid> /f /t`(プロセスツリー指定)でも終了しないことを実機で確認した(npx経由の子プロセスがツリーから外れてしまう模様)。放置すると次回実行時にポート衝突でテストがタイムアウトする。対策として、対象ポートを実際にLISTENしているプロセスを`netstat`で特定して直接killする`killByPort()`をどちらのテストにも実装済み。同種のスクリプトを新しく書く場合もこの関数を再利用すること。

**⚠️ 実際に事故った教訓(2026-09-12)**: `admin.e2e.mjs`は当初、テスト前に`staff.auth_user_id`を無条件で`null`に戻す後片付けをしていた。これにより、オーナーの実ログイン(`khs.w098style@gmail.com`)を當眞優希のstaffレコードに紐付けた**後**にこのテストを実行したところ、後片付け処理が実ログインの紐付けを`null`で上書きして消してしまい、ログインできなくなった。修正済み: テスト実行前の`auth_user_id`を必ず読み取って保存し、後片付けでは`null`固定ではなくその値に復元する方式に変更(`previousAuthUserId`)。**教訓: テストの後片付けは「初期値に戻す」のであって「決め打ちの値にする」のではない。実データが絡むテストを書く/直すときは、この非対称性に注意すること。**

**⚠️ もう1つのハマりどころ(2026-09-12、manage.html実装時)**: `serve`パッケージはデフォルトで「clean URLs」機能が有効で、`/manage.html?token=xxx`のようなリクエストを`/manage`に301リダイレクトし、その際**クエリ文字列ごと消してしまう**(`Location: /manage`、tokenが消える)。クエリ文字列が本質的に重要なページ(manage.htmlのような)をローカルテストする際は要注意。`lp/serve.json`に`{"cleanUrls": false}`を設定して解決した。

**設計判断(バックエンド単体テスト)**: `computeAvailability()`は元々Supabase呼び出し(`fetch*`ヘルパー)と空き枠計算ロジックが1関数に同居しており、そのままでは`SupabaseClient`をモックする脆いテストになってしまう。そこで空き枠計算の中核を`generateSlots()`という純粋関数(入力: 営業日・スタッフ一覧・シフトMap・既存予約Map・メニュー所要時間・現在時刻。Supabase型に一切依存しない)として切り出し、`computeAvailability()`はデータ取得後に`generateSlots()`へ委譲する薄いI/Oラッパーに変更した。`availability.test.ts`はこの`generateSlots()`を直接叩いており、境界値(最終受付ちょうど・施術時間が閉店をまたぐ・シフトと営業時間の交差・予約との重なり判定の隣接/包含/一部重複・過去枠の除外・複数スタッフのソート順)を実DBなしで網羅している。今後`_shared/`に新しいロジックを追加する際も、I/O(Supabase呼び出し)と純粋ロジックを分離してからテストを書く方針を踏襲すること。

## 実装履歴(2026-09-11〜2026-09-18)

- **LP**: 一通り完成(店内・スタイル写真反映済み)。WEB予約(`reserve.html`)自体は実装・デプロイ済みで稼働しているが、**2026-09-17時点ではLPからの導線を意図的に一時クローズ中**(オーナー説明の都合。「WEBで予約する」ボタンは非表示にし、「WEBでのご予約は近日公開予定です」という案内文に差し替えている。バックエンド・reserve.html自体は温存しており、リンクを戻すだけで即座に再公開できる)。電話番号は実際の値(`080-6481-0409`)に差し替え済み(2026-09-17。当初「電話予約は行っていない」とのことで一度LP全体から削除したが、店舗に確認したところ電話予約も受け付けるとのことで復元した。ヘッダー・ヒーロー・アクセス欄・CTA・フローティングボタンの計6箇所)
- **⚠️ デバイス優先度の確認(2026-09-13)**: 顧客向けLP・予約フロー(`index.html`/`reserve.html`/`manage.html`)は**スマホ利用が大多数になる想定**とユーザーが明言。`styles.css`/`reserve.css`のブレークポイント(860/720/560/520px)とE2Eテスト(420×900のスマホ相当ビューポート)で確認済みだが、今後この3画面に手を入れる際は必ずモバイル幅での見た目を優先して確認すること。逆に予約管理画面(`booking/admin/`)はPC専用で問題ない、とユーザーが確定(スタッフはPC/タブレットから利用する運用のため、モバイル最適化は不要)
- **営業時間の確認完了(2026-09-17)**: Instagram(@citydogs928)のbio「10:00〜19:00」は誤りで、正しくは既存のLP・seed.sqlの記載(平日10:00〜18:00・土日9:00〜18:00、いずれも最終受付)の通りと店舗に確認済み。コード変更は不要
- **予約UI(`lp/reserve.html`)**: メニュー選択→**担当スタイリスト選択(リストボックス、「指名なし(おまかせ)」がデフォルト)**→日時選択→お客様情報入力→確定、の3ステップウィザード。電話番号はハイフン自動整形。Playwrightで実ブラウザ・実APIに対してエンドツーエンドテスト済み(二重予約防止の除外、指名したスタイリストどおりに予約されることもUI経由で確認済み)
- **担当スタイリスト選択**: `staff`テーブルに行を追加するだけで(コード変更不要で)リストボックスに自動反映される設計。`GET /staff`(`role != 'assistant'`かつ稼働中のスタッフを返す)を新設し、`computeAvailability()`が元々持っていた`staffId`フィルタ引数と組み合わせて実現。現状スタイリストは1名のみだが、2人目以降が増えてもフロント・バックエンドとも改修不要
- **予約システム(バックエンド公開API)**: `menus` / `staff` / `availability` / `reservations` を実装・デプロイ・テスト済み。空き枠の時間刻みは30分(元15分から変更)。動作確認中に発見・修正したバグ: ①`--use-api`デプロイがimport mapを含むdeno.jsonを拾わずビルド失敗(インライン`npm:`指定子に戻して解決) ②`POST /reservations/:id/cancel`が顧客の知り得ない内部UUIDを要求していた設計バグ(`reservation_number`ベースに変更) ③型チェックで検出した埋め込みクエリの型不整合(2段階クエリに分割) ④完了画面で進捗インジケーターが「完了」にならない表示バグ(`hidden`属性とCSSの`display`指定の競合。`[hidden]{display:none!important}`を追加して解決)
- **予約管理画面(`booking/admin/`)— 実装済み**: Supabase Auth(email/password)によるログイン、日付×スタッフのスケジュール表示、予約番号/電話番号/氏名/日付範囲/ステータスでの検索を実装・デプロイ・テスト済み(実ログイン→実データでのE2Eテスト実施済み)。ログイン用アカウントは`khs.w098style@gmail.com`で作成済み(スタイリストのstaffレコードに仮で紐付け。将来的に店舗オーナー用として別レコードに分ける可能性あり)。**当初は「閲覧」機能のみだったが、2026-09-14〜16にかけて電話予約の代理登録・ステータス変更・リスケジュール・月次営業日/シフト設定・顧客管理まで実装済み。api-design.mdに記載の管理機能は2026-09-16時点ですべて実装完了**
- **予約管理画面が使う管理API(バックエンド)— 書き込み系の一部を実装(2026-09-14)**: `GET /admin-reservations`(検索)・`GET /admin-reservations/schedule`(スケジュール)に加え、`POST /admin-reservations`(電話予約の代理登録)・`PATCH /admin-reservations/:id`(ステータス変更・スタッフ/時間のリスケジュール)を実装・デプロイ済み。`admin-menus`(メニューCRUD)も実装済み。
  - 公開の`POST /reservations`と共通のロジック(空き枠再検証・EXCLUDE制約による競合検知)は`_shared/customers.ts`(顧客upsert)・`_shared/reservationEmail.ts`(確認メール本文)に切り出して重複を解消した
  - リスケジュール(`PATCH`でstaff_id/start_atを変更)は、変更対象の予約自身を空き判定から除外する`excludeReservationId`を`computeAvailability()`に追加して対応(除外しないと同じ時間のままスタッフだけ変える等のケースで自分自身とぶつかって誤って`SLOT_UNAVAILABLE`になる、実装時に発見した境界値)
  - **電話番号チェックは最終的に公開予約(`reserve.js`)と完全に同一仕様に統一(2026-09-14)**: 当初は電話受付を想定して固定電話も許容する`isValidJpPhone`を追加していたが、管理画面のハイフン自動整形もreserve.jsと揃えたいというユーザー判断を受け、バックエンドの検証も携帯限定の`isValidJpMobilePhone`に統一した(`isValidJpPhone`は削除、単体テストも整理済み)。固定電話からの代理予約登録は現状できない
  - **管理画面(`booking/admin/`)側のUIも実装済み(2026-09-14)**: スケジュールタブに「電話予約を登録」ボタン(モーダルで氏名・電話番号・メールアドレス任意・メニュー・担当・日時・メモを入力→`POST /admin-reservations`)を追加。スケジュール/検索両タブの各予約に「編集」ボタンを追加し、共通の編集モーダルから①ステータス変更(現在のステータスから遷移可能な選択肢のみをクライアント側でも絞り込んで表示。理由必須の遷移は理由入力欄を表示)②リスケジュール(担当・来店日・開始時刻の変更)を`PATCH /admin-reservations/:id`で行える
  - メニュー・スタッフの選択肢は`GET /menus`・`GET /staff`(いずれも公開API)から取得してキャッシュする方式
  - **⚠️ 実際に踏んだ教訓**: 当初「開始時刻」を自由入力の`<input type="time">`にしていたが、空き枠は30分刻みでしか生成されないため、その刻みに乗らない時刻(例: 11:15)を送信すると「その時刻の枠自体が存在しない」だけなのに「選択した時間はすでに埋まっています」という紛らわしいエラーになる不具合が実機で発生した。予約画面(`reserve.js`)と同様に**`GET /availability`を叩いて実際に予約可能な時刻だけを選択肢にする方式**に修正した。リスケジュール用の選択肢も同じ理屈で`GET /availability`を使うが、変更対象の予約自身が今使っている枠はこのAPIでは(自分自身とぶつかって)除外されてしまうため、担当・日付を変えていない場合は現在の枠を選択肢に補って表示している。この対応に伴い、`schedule.ts`・`list.ts`のレスポンスに`menu_id`を追加した
  - **🐛 実際に踏んだ教訓(2026-09-16、E2Eテストで発見した実害のあるバグ)**: 代理予約登録モーダルで「メニューを選ぶ→続けて日付を変える」のように短時間に連続して操作すると、それぞれの操作が空き枠取得(非同期)を呼ぶため、**後の操作(日付変更)によるリクエストより、前の操作(メニュー変更、まだ古い日付)によるリクエストの応答が遅れて返ってくることがあり、古い応答が新しい応答を上書きしてしまう**不具合があった。実機では「+14日後を指定したはずが今日の日付で予約が作られる」という形で再現した。E2Eテストを`.last()`(一覧の末尾要素を掴む、同姓同名の予約が複数あると曖昧になる)から、POSTレスポンスのidで一意に対象を特定する方式に直したことで初めて検出できた。`refreshCreateSlots()`・`refreshEditSlots()`双方に、呼び出しごとに増分するリクエストトークンを持たせ、自分より後のリクエストが既に完了していれば結果を破棄するガードを追加して解決。**教訓: 複数のUI操作がそれぞれ独立した非同期処理(特に選択肢の取得)を発火させる作りにする場合、リクエストの完了順序は保証されないことを前提に設計すること。E2Eテストで要素を特定する際も、名前など重複しうる属性ではなく一意なID(サーバーが発行したもの)を使うべき**
  - **🐛 実際に踏んだ教訓(2026-09-16、後日の別作業で発覚)**: リスケジュール先に固定の「実行日+15日」を使っていたところ、同日中に8回以上実行し直した結果、テスト予約が積み上がって当日の枠が完全に埋まり、「空き枠がありません」でテストが失敗した。応急で+40日に変えたところ今度は定休日(月曜)に当たって別の形で失敗し、固定オフセットそのものが根本的に脆いと判断。**最終対応**: `findDateWithRealOptions()`ヘルパーを新設し、日付を1日ずつ進めながら実際に空き枠が見つかる日まで探す方式(+30日から開始、最大15日分試す)に変更した。**教訓: 日付が絡むE2Eテストで固定の相対オフセットを使う場合、(a)後片付けしない予約が積み上がる、(b)たまたま定休日に当たる、の両方が起こりうる。決め打ちの日付ではなく、実際に条件(空き枠がある等)を満たす日を探す方式を最初から使うこと**
  - **この過程で発見・修正したテスト自体の不具合3件**: ①`card.innerText()`では`<input value>`の値が拾えず常に誤判定していた(`inputValue()`に修正) ②`.last()`で削除後のdetached判定をしていたため他の要素が残っていると永久に検知できなかった(`data-id`指定のロケータに変更) ③追加直後に固定時間だけ待って一発判定していたため、Edge Functionの応答が遅いと誤って失敗していた(ポーリング方式に変更)
- **シフト/営業日設定(バックエンド)— 実装・デプロイ済み(2026-09-16)**: `admin-business-days`・`admin-staff-shifts`を実装。
  - `PUT`メソッドを初めて使うAPIだったため、`_shared/cors.ts`の許可メソッド一覧に`PUT`を追加(全11 Functionを再デプロイ)
  - `generate-month`は「まだ行が無い日だけ」デフォルト値で埋める設計(`ON CONFLICT DO NOTHING`)
  - **⚠️ 実際に踏んだ教訓**: 新しいFunctionをデプロイした直後、数十秒ほど`401`(期待通り)と`404`(Supabase基盤側の"Requested function was not found")が不規則に混在する現象を実機で確認した。エッジネットワークへの伝播待ちと見られ、待てば自然に安定する
  - **管理画面(`booking/admin/`)側のUI(2026-09-16)**: 新しい「営業日・シフト」タブを追加。**初版は当月の全日付をカードで縦に並べる一覧UIだったが、ユーザーから「30日分をベタッと並べるのは使いづらい」と即フィードバックを受け、SALON BOARD相当のカレンダーグリッド表示に作り直した**。**教訓: 「1か月分の設定」のような月次データは、最初から縦並びリストではなくカレンダー形式を検討すべきだった**
- **予約管理リンク(顧客ログイン不要)— 実装済みだが実際には未稼働**: Web予約時にメールアドレスを必須化し、予約ごとに`manage_token`(uuid)を発行。予約確定メール(Resend経由)に「確認・変更・キャンセルリンク」を記載し、`manage.html?token=...`でログインなしにその予約1件だけを照会・キャンセルできる。`MANAGE_PAGE_BASE_URL`が未設定のため、メール内のリンクは`https://your-domain-not-configured.example/manage.html`というプレースホルダーURLのまま送信される(`reservations/create.ts`の意図的なフェイルセーフ)
- **LPコンテンツ管理画面 — 実装・デプロイ・実機テストまで完了(2026-09-13〜14)**: 別アプリを新設せず、既存の予約管理画面(`booking/admin/`)に「LPコンテンツ」タブとして統合した。対象はCONCEPT・SHOP & STYLE・MENU & PRICE・STAFFの4つ。
  - **スタッフ管理も同じタブに統合(2026-09-13追加)**: 「オーナーにDBの`staff`テーブルへの直接行追加を求めるのはハードルが高い」というフィードバックを受けて、氏名・権限区分・稼働状況・表示順の編集と新規スタッフの追加も同じエンドポイントに統合した
  - **スタッフ・メニューの削除機能を追加(2026-09-16)**: 当初は「削除は一切提供しない」設計だったが、テストで登録したものを消せないと困るという指摘を受け見直した。実際の制約は外部キー制約であり、一度でも予約に使われた行だけが物理削除できない。「削除を試みて、外部キー違反(`23503`)ならエラーメッセージで案内」方式に変更。**教訓: 「削除不可」という制約を実装する際は、本当にDB制約でそうなるケースだけに絞り込めないか(=一律で機能ごと封印していないか)を疑うこと**
  - **画像アップロード機能 — 実装済み(2026-09-18)**: 当初は「パスを手入力する」運用を意図的な設計としていたが、実際には画像の実配置(git commit+再デプロイ)を開発者に依頼する必要があり、オーナー自身で完結できていなかった。Supabase Storageへの実アップロード機能を実装した(詳細はbooking/design/data-model.md「8.」参照)
  - **既知の副作用**: メニュー価格表示から「〜10,000」のような価格帯表示が失われた(`menus.price`が単一整数のため)
  - **⚠️ 実際に踏んだ教訓(2026-09-13)**: マイグレーション適用直後は`db push --include-seed`がseed実行を「成功」と報告してもデータが実際には反映されないことがあった。新テーブル作成直後はPostgREST側のスキーマキャッシュが追いつかず数十秒だけ500を返すことがある
  - **⚠️ 実際に踏んだ教訓(2026-09-14)**: `_shared/cors.ts`の`Access-Control-Allow-Methods`に`DELETE`を含め忘れており、削除だけブラウザのpreflightでブロックされる不具合が実機で発生した。**教訓: 新しいHTTPメソッドを追加するAPIを実装する際は、CORS共有設定の許可メソッド一覧を更新し忘れていないか必ず確認すること**
- **顧客管理 — 実装・デプロイ済み(2026-09-16)**: `admin-customers`を実装。管理画面に「顧客管理」タブを追加
- **Google口コミ連携 — 実装後に撤回、手動更新方式へ切り替え(2026-09-17)**: 2026-09-14に配線・2026-09-17に本番稼働までさせたが、**Google Maps Platformの利用規約上、rating/review_countのような評価データの自社DBへのキャッシュ・保存は規約違反の可能性が高い**(request it live, display it, do not warehouse it)ことが判明。都度ライブ取得に切り替えると無料枠(月1,000回)を日割りで33回程度しか使えずコスト管理と両立しないため、ユーザー判断で連携自体を完全に撤回。`google-rating` Function・`google_rating_cache`テーブルを物理削除し、評価スコア・口コミ件数を管理画面から手動更新する方式(`site_rating`テーブル)に変更した。**教訓: 外部API連携を実装する際は「コスト」だけでなく「利用規約上のデータ保存・キャッシュ制限」も実装前に確認すること**
- **🚨 セキュリティ修正(2026-09-16)— 全テーブルでRow Level Security(RLS)が未設定だった**: Supabase Security Advisorの警告をきっかけに発覚。プロジェクト開始時からどのテーブルにもRLSが一度も有効化されておらず、anon keyだけでPostgREST自動API経由に顧客情報を含む全テーブルを直接読み書きできる状態だった。実際にanon keyで`customers`テーブルが直接読み取れることをcurlで実証してから修正、全テーブルにRLSを有効化(ポリシーは作らず遮断するだけ)。**教訓: 「アプリ層でPostgREST直叩きを禁止する設計方針」はDB側の強制力を伴わないため、テーブル作成時は必ずRLSも同時に有効化する習慣が必要**
- **公開APIのレート制限 — 実装・デプロイ済み(2026-09-16)**: 固定ウィンドウ方式のレート制限を実装(`rate_limit_hit()` RPCで原子的にカウント)。fail-open方針(RPC失敗時は素通し)
- **Cloudflare Turnstile — 実装・デプロイ・実機確認済み(2026-09-16)**: `POST /reservations`にボット対策を追加。
  - **⚠️ 実際に踏んだ教訓**: `<script ... async defer>`はHTML仕様上`async`が優先されて`defer`が無視される。**教訓: `async`と`defer`を両方指定しても`defer`にはならない。実行順序を保証したい外部スクリプトには`defer`のみを使うこと**
  - **⚠️ 実際に踏んだ教訓(本番キーとテストキーは組み合わせを崩せない)**: 本番用サイトキーは実際にボット検知を行うため、Playwrightのヘッドレスブラウザが正当にボットとして検知され永久にトークンが発行されない。テスト用サイトキー・シークレットキーは必ずペアで使う必要があり、`reserve.e2e.mjs`実行前後で手動により一時的にテスト用シークレットキーへ切り替える手順が必要(冒頭のコメント参照)
- **コードレビュー(第2回、別視点)で発見した2件のHIGHを修正(2026-09-17)**:
  1. **公開予約フォームからの顧客情報上書き**: `upsertCustomerByPhone()`が認証なしで既存顧客の氏名・emailを無条件上書きしていた穴を`allowOverwrite`フラグで解消(公開予約は上書きしない、電話予約代理登録のみ上書き)
  2. **予約キャンセル・ステータス更新の非原子的処理(TOCTOU競合)**: 3箇所のUPDATE文に条件付きWHERE句を追加し原子的な更新に変更
  - **教訓: 「全体を網羅的に洗う」レビューと「業務ロジックを横断して弱点を探す」レビューは観点が違うため、両方やって初めて見つかる不具合がある**
- **呼び出し元のない公開エンドポイントを削除(2026-09-18)**: `GET /reservations/lookup`・`POST /reservations/:reservation_number/cancel`が、LP・管理画面のどちらからも一度も呼ばれていない認証なしの公開APIであることが判明し削除。管理画面の「予約検索」タブで実質カバーされていた
- **スタッフの休憩時間を空き枠計算から除外する — 実装・デプロイ済み(2026-09-16)**:
  - **⚠️ 実際に踏んだ教訓**: デプロイ直後、ソースが正しくデプロイされているにもかかわらず数分間古いコードのレスポンスが返り続ける現象が発生
  - **🚨 実際に踏んだ教訓(より重い症状)**: `admin-staff-shifts`だけ`deploy`が3回とも成功表示なのに実際には一度も再デプロイされていなかった。**対処法: `supabase functions delete <name>`で削除してから`deploy`し直すと解消**。**教訓: `deploy`コマンドの「成功」表示だけを信用せず、重要な修正をデプロイした直後は`functions list`でversion/updated_atが実際に変化したか確認する習慣をつけること**
  - **🐛 E2Eテストで実際に踏んだ教訓**: 「モーダルを閉じる」と「一覧の再読み込み」が別の非同期処理になっているUIで、固定waitのせいで再読み込み前の古い状態を掴んでしまう競合状態が発生。`reopenUntil()`ヘルパー(期待する状態になるまでポーリング)で解決
  - **🐛 E2Eテストで実際に踏んだ教訓その2**: Postgresの`time`型は秒付きでJSON化されるため、`bs === '13:00'`という秒なし厳密一致が永久に失敗していた。アプリ側の表示コードに合わせ`bs.slice(0,5)`で比較するよう修正
  - **休憩対応のレビュー中に発見した既存の設計ギャップも解消(2026-09-16)**: `GET /availability`に`exclude_reservation_id`クエリパラメータを追加し、リスケジュール画面のフロントエンド手動補完コードを削除。**教訓: 場当たり的なフロントエンドの補完コードを見つけたら、サーバー側に既に用意されていて配線されていないだけの仕組みがないか疑うこと**
- **ホスティング先をCloudflare Workers(静的アセット配信)に決定・LP/管理画面ともデプロイ済み(2026-09-16)**:
  - **⚠️ 重要な確認結果**: Cloudflare Workersは`/manage.html`を`/manage`に307リダイレクトするが、クエリ文字列(`?token=...`)はリダイレクト時にきちんと保持されることを確認した(ローカルの`serve`パッケージで踏んだ不具合はCloudflare Workersでは発生しない)
  - **教訓**: `wrangler deploy --dry-run`はCloudflare APIとの通信をスキップするため、`.assetsignore`の効果を検証する手段としては信頼できない。実際に確かめるには本物のデプロイが必要
- **文字化けテストデータの発見・削除(2026-09-18)**: 管理画面のスケジュール表示で、Windowsターミナルからcurlで日本語(「テスト太郎」)を含むリクエストを直接送信した際、コンソールの文字コード(非UTF-8)によりバイトレベルで壊れた顧客名(`�`を含む)がDBに保存されていたことが発覚。該当の顧客・予約レコード2件を削除。ブラウザ経由の実際の予約フロー(fetch+JSON.stringify)は常に正しいUTF-8を使うため、この文字化けはWindowsターミナルでの手動curlテスト特有の問題であり、実際の利用経路では発生し得ない
- **他店舗展開の準備(2026-09-16、GitHubへの初回プッシュ前に実施)**: 接続情報を`lp/js/config.js`・`booking/admin/js/config.js`の2ファイルに集約。店舗固有ファイルの先頭に`🏪 店舗固有`コメントを付与(`grep -rn "🏪"`で一覧可能)。**予約確認メールの件名(`reservationEmail.ts`)に店舗名が直接ハードコードされていたのは、初回の洗い出しで見落としていた箇所**(ユーザーからの再確認指摘で発覚)。**教訓: 「店舗固有情報の洗い出し」はフロントエンド(HTML)だけでなく、メール本文・件名のようにバックエンドが生成する顧客向け出力も対象に含めること**
- **🐛 指名なし予約でアシスタントが混入するバグを発見・修正(2026-09-18)**: 管理画面のスケジュール表示で、LPの「指名なし(おまかせ)」で予約したはずのお客様がアシスタントスタッフに割り当てられているのをユーザーが発見。原因は`GET /staff`(予約画面の指名リスト)は`role != 'assistant'`で正しく絞り込んでいたが、空き枠計算の内部関数`_shared/availability.ts`の`fetchStaff()`にはこの絞り込みが漏れていたこと。指名なし時に全稼働スタッフ(アシスタント含む)が空き枠計算の対象になり、①同じ時刻の枠がスタイリスト分・アシスタント分で重複表示される ②「指名なし」がアシスタントに割り当てられる、という2つの不具合が起きていた。`fetchStaff()`に`neq("role", "assistant")`を追加して解消(`availability`・`reservations`・`admin-reservations`を再デプロイ、実機で確認済み)
  - **教訓: 複数箇所で「同じはずの絞り込み条件」を独立に実装すると、片方だけ更新されて食い違う典型例。`GET /staff`と`_shared/availability.ts`の`fetchStaff()`は本来同じルールを共有すべきだった**
- **「指名なし(おまかせ)」を廃止、担当スタイリストの指名を常に必須に変更(2026-09-18)**: 上記バグの調査中、そもそも「指名なし」はUI上も設計上も問題を抱えていることが判明した。`lp/js/reserve.js`は指名なし時の空き枠を時刻でグルーピングせず、同じ時刻のボタンを担当者ごとに複数表示するだけで、お客様には各ボタンがどの担当者に対応するか一切分からない(表示は時刻のみ)。つまり「システムが自動で割り当てる」のではなく「お客様が見た目の区別がつかないボタンをたまたま選んだ担当者になる」という実態だった。さらに「このメニューはAさんしか担当できない」のようなスタッフ×メニューの担当可否を制限する仕組みも存在しないため、指名なしで意図しない担当者に割り振られるリスクがある。ユーザー判断で「指名なし」の概念自体をLP・管理画面の両方から完全に削除することにした。
  - **フロントエンド**: `lp/reserve.html`の`#staffSelect`から`指名なし(おまかせ)`オプションを削除し`required`に。`booking/admin/index.html`の電話予約登録(`#crStaff`)・リスケジュール(`#editStaffSelect`)も同様。`admin.js`の`renderSlotOptions()`は指名なし用の重複排除・担当者名併記ロジックが不要になったため削除(常に1名分の空き枠しか返らないため)
  - **バックエンド**: `GET /availability`の`staff_id`をクエリ必須化(未指定は`VALIDATION_ERROR`)。`POST /reservations`・`POST /admin-reservations`の`staff_id`も`requireNonEmptyString`で必須化。`PATCH /admin-reservations/:id`(リスケジュール)は「担当を変えずに日時だけ変える」ケースがあるため、`staff_id`省略時は現在の担当を維持する仕様のみ残した(「担当を空にする」は非サポート)。`_shared/availability.ts`の`ComputeAvailabilityParams.staffId`・`fetchStaff()`の型も`string`必須に変更し、「指名なしで空き枠0件を返す」旧分岐を削除
  - 型チェック・Lint・単体テスト(65件)すべてグリーン。将来もし「メニューごとの担当可能スタッフ制限」や「指名なし時の割り当てロジック(例: 均等割り当て・空き枠が最も多い担当者を優先)」を作る場合は、この撤去内容を踏まえて設計し直すこと

## メニュー区分・複数選択・オプション対応(2026-09-24)

オーナーが全メニューをExcel(`booking/reference/BARBER_City_Dogs_メニュー価格一覧.xlsx`)に整理したのを受け、予約画面から「カット単体」「パーマ単体」「カット+パーマ+顔剃り」のような組み合わせを選べるようにした。

- **DB(0012)**: `menus`に`category`(cut/color/perm/option)・`price_is_from`(「〜」付き下限価格)を追加。`reservation_items`(予約ごとのメニュー内訳+価格/所要時間/`price_is_from`スナップショット)を新設し、既存予約はバックフィル。旧4メニュー(複合メニュー)は`is_active=false`で温存。`reservations.menu_id`は「主メニュー」(優先 カット>カラー>パーマ)として残し、`price_at_booking`・`time_range`は合計値。
- **選択ルール**(サーバー`_shared/menuSelection.ts`で強制、`reserve.js`/`admin.js`は同じルールをUX目的で再現): カット/カラー/パーマは各区分1つまで、この3区分のいずれか1つ必須、オプションは追加のみ(単独不可)。**料金・時間は単純合算、セット割なし**(オーナー判断)。
- **所要時間は仮置きの推定値**(seed.sql冒頭コメント参照。カット40/メンテ30/高校生40/中学生以下30/カラー60/パーマ90/ツイスト120/オプション各10分)。店舗が実時間を確認でき次第、管理画面「メニュー・料金」で更新する。HotPepperのクーポンページから時間抽出は試みたができなかった。
- **Excel由来の注意**: LPの「すべてのメニューに眉メンテナンスが含まれています」は誤記とオーナー確認済み。眉毛整えはオプション(¥500)。LPの特徴カード2/3の文言はseed.sqlで更新したが、**本番の既存データはseedでは上書きされない**ため管理画面「LPコンテンツ」から直接修正が必要(カード3に旧テスト文言が残っている)。
- **API**: `GET /availability`は`menu_ids`(csv)を推奨、旧`menu_id`は後方互換、リスケジュール用に`duration_minutes`(1〜600)も受ける。`POST /reservations`・`POST /admin-reservations`は`menu_ids`配列(旧`menu_id`も可)。`_shared/availability.ts`に`computeSlotsForDuration()`を追加(リスケは予約の現在の長さで枠検索するため)。
- **既知の制約**: `insertReservationItems()`は`reservations`の挿入後に内訳を挿入し、失敗時は補償として予約を削除する(トランザクションではない)。稀に削除も失敗すると内訳のない予約が残るが、その場合も主メニュー名にフォールバック表示される。
- **教訓**: 作業中に他ファイル(`reservations/create.ts`等)がディスク上で変更されていた(指名なし廃止)ため、編集前に必ず再読み込みした。

### 🐛 seed再実行でSHOP & STYLEの写真が重複(2026-09-24)
メニュー追加のため`seed.sql`を本番に流したところ、LPのスタイル写真が3枚→6枚に重複表示された。原因: seedの重複判定が「静的パス(`images/style-*.jpg`)の行が無ければ追加」だったが、本番は管理画面でStorage画像に差し替え済みで静的パスの行が存在せず、4行(店内1+スタイル3)が新規追加された。追加分4行のみIDを指定して削除して復旧(アップロード済みの行・Storage実体には触れていない)。`site_features`も同じ「タイトル一致」判定だったため、`site_features`は全体が空のとき、`site_gallery_photos`は種別(interior/style)ごとに空のときだけ追加する方式に変更。**教訓: 管理画面で編集されうるデータのseedは「値が一致する行が無ければ追加」ではなく「テーブル(区分)が空のときだけ追加」にする。本番に流す前に、既存データとの突き合わせでどの行が追加されるかを確認すること。**

### メニュー所要時間を店舗回答で確定(2026-09-25)
店舗の回答: カットは子供含め60分/カラー60分/パーマ・ツイスト各60分/オプション(顔剃り・眉毛・ノーズ)10〜15分。組み合わせも通常は単純加算(セット割はクーポン時のみ)。本番DBは`update menus`で更新済み、seed.sqlも同値に更新。オプションは範囲回答のため上限の15分を採用。E2Eの期待値(カット+顔剃り)は50分→75分に変更。**未確認**: フェード メンテナンスカット(30分の仮値のまま)、「カット+パーマ+ツイスト=180分」の意図(パーマとツイストの併用か)、施術後のインターバルの有無。予約はバッファなしで隙間なく入る(`EXCLUDE`制約は重なりのみ禁止、隣接は許可)。

### 追加回答の反映(2026-09-25)
- **施術後のインターバル(バッファ)は不要**と店舗回答。実装しない(検討内容: スタッフごとの`buffer_minutes`案。枠が30分刻みのため10分程度のバッファは実質1枠消えるので、入れるなら0か30分、刻み変更は予約ボタンが倍になりUI的に微妙、という整理をした)。フェード メンテナンスカットは60分に確定(DB・seed更新)。
- **パーマとツイストは別メニューだが、ほぼ併用で注文される**ため、選択ルールを「カット・カラーは各区分1つまで、パーマ区分は複数可」に変更(`_shared/menuSelection.ts`の`SINGLE_SELECT_CATEGORIES`、LP予約画面・管理画面の電話予約登録も同様)。カット+パーマ+ツイスト=180分(60×3)の店舗回答と単純加算が一致する。DBのCHECK制約は区分の値のみなので、マイグレーションは不要。

### 文面の全体見直し(2026-09-25)
所要時間・選択ルール確定後に、顧客向け/管理画面の文言を新メニュー体系に揃えた。①LPのmeta descriptionにカラー・パーマを反映 ②予約画面の案内文・区分ごとのヒント(カットは1つまで、パーマ・ツイストは片方でも両方でも可)・合計表示の説明を更新 ③管理画面のメニュー説明と区分ラベル(「1予約で1つまで」「併用可」)を更新 ④LPの特徴カード2・3(旧: 「カット+眉メンテのメニューもご用意」)を「オプションの眉毛整え/顔剃りをカットなどにプラスできます」に変更(価格は書かない: 改定で古くなるため) ⑤説明文が名前の単なる繰り返しだったメニュー(高校生カット・中学生以下カット・パーマ・顔剃り)は説明なしにし、LPは説明が空なら空の`<p>`を出さない。本番DBは変更前の文言に一致する行のみ更新(管理画面で編集済みの内容を上書きしない)。seed.sqlも同内容。

### 「〜」付きメニューの実際の会計金額を記録(2026-09-25)
オーナーの指摘「「〜」付きメニューがあるので、予約時点では料金が確定しない。売上実績もあるのだから、実際の金額を入力する欄が要るのでは」を受けて実装。従来は売上の見込み・実績とも予約時点の下限価格(`price_at_booking`)で集計しており、会計完了にしても実際の金額を残せなかった(値引き・クーポンのずれも同様)。
- **DB(0013)**: `reservations.final_price integer check (>=0)`(NULL可)。既存の予約は全てNULLのままで挙動不変。
- **入力ルール(`_shared/checkout.ts`、純粋関数+単体テスト)**: 設定できるのは`completed`の予約のみ(他ステータスへの変更と同時・completed以外への単独設定は拒否)、「〜」付きを含む予約を`completed`にする時は必須、0以上1,000万以下の整数(0=無料対応は有効)、`null`での取り消しは不可。「〜」付きかの判定(`reservation_items`の問い合わせ)は「completedへの変更で金額未指定」の時だけ行う。
- **集計(`_shared/revenue.ts`)**: completedで`final_price`があれば見込み・実績ともそれを使い、なければ`price_at_booking`。会計金額が未確定の「〜」付き予約を含む集計は`forecast_has_estimate`/`actual_has_estimate`が`true`になり、管理画面の売上タブは「¥13,500〜 ※下限額で集計」と表示する。顧客管理の予約履歴の金額も会計金額を優先。
- **管理画面**: 予約編集モーダルのステータス変更で「完了」を選ぶと「実際の会計金額」欄が出る(「〜」なしは予約時点の金額が初期値、「〜」付きは下限のまま保存されないよう空欄から始めて必須)。完了後は「会計金額」セクションから修正できる。スケジュール等のカードは確定金額があればそれを表示。
- **意図的にやらなかったこと**: `manage.html`(顧客向け)の表示は変更していない(キャンセル可否の確認ページで、完了後の金額を見せる必要が薄い)。管理画面E2Eでは「〜」付き予約の必須入力の経路を通していない(単体テストで担保。E2Eは「〜」なしの予約で入力・不正入力・完了後の修正を確認)。

### 納品前クリア時の注意: 旧セットメニュー4件の削除(2026-09-25)
0012で非公開にした旧セットメニュー4件(メンズカット + 眉毛整え / カット + シェービング / カット + メッシュカラー + 眉毛整え / カット + パーマ + 眉毛整え)は、過去の予約(テスト予約が大半)が`reservations.menu_id`で参照しているため、管理画面からも物理削除できない。納品前にテスト予約を一括削除する際、**予約を先に削除してから**(`reservation_items`は`on delete cascade`で連動して消える)、この4件を削除すれば管理画面のメニュー一覧からも消える。実際の(テストでない)予約が旧メニューに残っている場合は、削除せず非公開のままにする。

### 管理画面のメニュー一覧をコンパクト化(2026-09-25)
旧セットメニュー4件が非公開のまま同じ大きさのカードで並び、Menu & PRICEタブが縦に長かったため。①公開中のメニューは通常表示、非公開のメニューは「非公開のメニュー(N件)」の折りたたみ欄(既定は閉じる。再読み込みしても開閉状態は維持)にまとめた ②メニューの編集カードを、10項目の縦積みから12列グリッドの2行(1行目: 名前・区分・価格・所要・表示順 / 2行目: 説明・「〜」・公開・保存/削除)に変更し、1枚あたりの高さを約1/3にした(900px以下では折り返し)。保存で公開/非公開を切り替えたら、正しい欄へ移すため一覧を再描画する。他のカード(特徴カード・写真・スタッフ)は変更していない。

### 管理画面に推奨枚数の注意書きを追加(2026-09-25)
CONCEPT(特徴カード)・SHOP & STYLE(スタイル例)は上限なしで追加できる仕様のまま、管理画面の各セクション冒頭に推奨枚数(3〜6枚、6枚が最もきれい)と、画面幅ごとの列数(PC3列/タブレット2列(スタイル例のみ)/スマホ1列)、店内メイン写真はLPに1枚だけ表示される旨、画像は1枚5MBまでの旨を注記した。上限をコードで強制する機能は、必要になるまで作らない方針。

(2026-09-25追記) 管理画面のメニュー説明文に、非公開にしたメニューは「非公開のメニュー」折りたたみ欄へ移動し、そこから再公開できる旨を追記した。
