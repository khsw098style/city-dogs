# TESTING.md — テストの内容と実行方法

このファイルは、City Dogs の自動テストが**何を・どの順番で確認しているか**を、コードを読まなくても分かるようにまとめたものです。

> **⚠️ 更新ルール**: E2Eテスト(`lp/tests/*.e2e.mjs`・`booking/admin/tests/admin.e2e.mjs`)や単体テストの確認内容を変えたら、**必ずこのファイルも同じ変更で更新する**(テストを直したのに、ここが古いままになるのを防ぐため)。最終更新: 2026-09-25。

---

## 1. テストの種類と実行方法

| テスト | 何を見るか | 実行方法 | 必要なもの |
|---|---|---|---|
| ① LP予約 E2E | お客様がスマホで予約する流れ | `cd lp` → `npm run test:e2e` | Turnstileのテスト用キー(下記) |
| ② LP表示 E2E | トップページがDBの内容で正しく描かれるか | `cd lp` → `node tests/site-content.e2e.mjs` | なし |
| ③ 管理画面 E2E | スタッフがログインして操作する流れ | `cd booking/admin` → `SUPABASE_SERVICE_ROLE_KEY=<キー> npm run test:e2e` | service_roleキー、Turnstileのテスト用キー(下記) |
| ④ 単体テスト | サーバー側のロジック(DB接続なし) | `cd booking` → `npm run test` | なし |

初回のみ、`npm install` と `npx playwright install chromium` が必要です。

### 実行前に知っておくこと
- **本番と同じSupabaseに対して動きます**。テスト用の予約が本番DBに残ります(納品前にまとめて削除する運用)。
- ①の後半(DBの確認・`manage.html`)は、`SUPABASE_SERVICE_ROLE_KEY` を付けたときだけ実行されます。付けないと、その部分はスキップされます。
- service_roleキーはコードに書かず、環境変数で渡します。
- 実データに触るテストの後片付けは「決め打ちの値」ではなく「実行前の値に戻す」ことです(例: ③はスタッフの紐付けを、テスト前の状態に復元します)。

### Turnstile(ボット対策)のテスト用キーの切り替え
本番のボット対策は、自動テストのブラウザを弾いてしまいます。テストの間だけ、Cloudflareが公開している「常に成功する」テスト用キーに切り替えます(サイトキーはテストが自動で差し替えます。**シークレットキーは手動で切り替えます**)。

| 対象 | 切り替える場所 | 方法 |
|---|---|---|
| ①(LPの予約フォーム) | Edge Functionのsecret `TURNSTILE_SECRET_KEY` | `cd booking` → `npx supabase secrets set TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"` |
| ③(管理画面のログイン) | Supabaseダッシュボード → Authentication → Attack Protection の「Captcha secret」 | 同じテスト用キーに書き換えて保存 |

**テストが終わったら、必ず本番キーに戻す**(戻し忘れると、本番のボット対策が「常に成功」のままになる)。本番キーはCloudflareのTurnstile画面で確認できます(キーそのものはこのファイルに書きません)。`npx supabase secrets list` の値のハッシュを比べれば、戻ったかどうかを確認できます。

---

## 2. ① LP予約 E2E(`lp/tests/reserve.e2e.mjs`)

お客様がスマホ(画面幅420px)で予約する流れを、実際のブラウザで操作します。

1. **準備**: ローカルのサーバーを起動し、ボット対策をテスト用キーに差し替えて、予約画面を開く。
2. **メニュー選択の動作確認**
   - 何も選んでいない間は「次へ」が押せず、オプションも表示されない。
   - カットを選ぶと、オプション(顔剃りなど)が表示される。
   - カット+パーマの合計が「¥9,500〜」になる(パーマは「〜」付きなので合計も「〜」)。
   - さらにツイストを選ぶと「¥15,500〜・約180分」になり、パーマの選択は外れない(パーマ・ツイストは併用できる)。
   - 別のカット(高校生カット)を選ぶと、最初のカットが外れる(カットは1つだけ)。
   - カット+顔剃りの合計が「¥4,800・約75分」になる。
   - カットを外すと、顔剃りも一緒に外れ、「次へ」が押せなくなる(オプションだけの予約はできない)。
3. **担当と日時**: 担当スタイリストを指名する(必須)。7日以内で空き枠のある日を探し、空き枠が表示される。
4. **お客様情報の入力と送信**: 名前・電話番号・メール・要望を入力し、ボット対策の確認が終わるのを待って送信する。
5. **完了画面の確認**: 指名したスタイリストが担当者として表示され、メニュー名「カット + 顔剃り」と金額「¥4,800」が表示される。
6. **(service_roleキーがある場合)裏側の確認**
   - DBに、予約の内訳が2件(カット・顔剃り)・合計4,800円で保存されている。
   - 予約確認ページ(`manage.html`)に、予約番号と複数メニューが表示される。
   - そのページから予約をキャンセルできる。
   - 不正なトークンでは、エラー表示になる。
7. **最後に**: ブラウザのコンソールにエラーが出ていない。

## 3. ② LP表示 E2E(`lp/tests/site-content.e2e.mjs`)

トップページがDBの内容で正しく描かれているかを確認します。予約はしません。

1. CONCEPT(特徴カード)が3枚表示される。
2. SHOP & STYLE の店内写真とスタイル写真が、APIの内容と一致する(写真は管理画面で差し替わるため、決め打ちではなくAPIと突き合わせる)。
3. MENU & PRICE が10件、区分の見出し(カット・カラー・パーマ・オプション)の順に並ぶ。先頭は ¥4,000、カラーは「¥4,500〜」。
4. STAFF が2名表示され、肩書き(理容歴4年)が反映されている。
5. 評価バッジ(★スコア・口コミ件数)が、APIの値と一致する(管理画面で手動更新される値なので、決め打ちにしない)。
6. ブラウザのコンソールにエラーが出ていない。

> **注意**: 1・3・4 は「メニュー10件・特徴カード3枚・スタッフ2名」と件数を決め打ちしています。オーナーが管理画面でこれらを増減すると、機能は正常でもこのテストは失敗します。運用開始後は、決め打ちを外すことを検討する。

## 4. ③ 管理画面 E2E(`booking/admin/tests/admin.e2e.mjs`)

スタッフが管理画面にログインして操作する流れを確認します。

### 準備とログイン
- **0. 前回の残りを掃除**: 前回失敗して残ったテスト用データを削除する。
- **1. テスト用ユーザーの作成**: テスト用のログインユーザーを作り、既存のスタッフに紐付ける。
- **2. ログイン**: ボット対策の確認が終わるのを待ってログインする。

### 表示の確認(軽め)
- **3. スケジュールタブ**、**4. 検索タブ**: 画面が開くことの確認。スクリーンショットを撮り、検索件数を表示するだけで、中身の細かい確認はしていない。

### LPコンテンツの編集(それぞれ「追加 → 編集 → 削除」)
- **5.5. 評価バッジ**: 編集して保存し、ページを再読み込みしても反映されている(元の値はテスト後に復元)。
- **6. CONCEPT(特徴カード)**
- **7. SHOP & STYLE(写真)**
- **8. MENU & PRICE(メニュー)**: あわせて、「非公開のメニュー」の折りたたみ欄が最初は閉じていること、公開中のメニューがその欄の外にあることを確認する。
- **9. STAFF(スタッフ紹介)**

### 予約の操作
- **10. 電話予約の代理登録**
  - 主メニューを選ぶと、追加メニュー・オプションの選択肢が出る。オプションを選ぶと、合計と所要時間が表示される。
  - 担当スタイリストを選び、日付を入れると、実際の空き枠から選べる。
  - 電話番号を「ハイフンなし」で入れると、自動でハイフン付きに整う。
  - メールは空のままでも登録できる。
  - 予約が、連結したメニュー名(例: カット + 眉毛整え)で登録される。
- **11. ステータス変更**: 確定 → 施術中。
- **12. リスケジュール**: 担当はそのままで、空きのある別の日の枠へ移動する。
- **12b. 会計金額**: 会計待ち → 会計完了。
  - 会計完了以外への変更では、会計金額の入力欄が出ない。
  - 会計完了を選ぶと入力欄が出て、初期値は予約時点の金額になる。
  - 数字でない入力は、エラー表示で弾かれ、ステータスは変わらない。
  - 「3,900」(カンマ付き)を入力して完了にすると、カードに「¥3,900」と表示される。
  - 完了後は、ステータス変更フォームが消え、「会計金額」の保存欄から金額を 4,100 円に修正できる。

### 営業日・シフト・顧客・売上
- **13. 営業日・シフト**
  - 営業日を一括生成し、1日を編集(メモ)して、開き直しても保存されている。
  - スタッフのシフトを一括生成し、1日を編集(メモ・休憩時間 13:00〜14:00)して、開き直しても保存されている。
  - 休憩時間に重なる空き枠が、予約の空き枠(`GET /availability`)から除外されている。
- **14. 顧客管理**: 電話番号で検索 → メモと「要注意」フラグを編集して保存 → 予約履歴に、10で登録した予約が表示される。
- **15. 売上予定・実績**: スタッフごとの列が表示され、10で登録した予約が「見込み件数」に反映されている。

### 最後に
- ブラウザのコンソールにエラーが出ていない(失敗時は、その瞬間の画面のスクリーンショットも残す)。
- **16. 後片付け**: テスト用のデータとログインユーザーを削除し、評価バッジとスタッフの紐付けを、**テスト前の値**に戻す。電話予約の代理登録で作った予約だけは、納品前の一括削除に任せて残す。

## 5. ④ 単体テスト(`booking/supabase/functions/**/*.test.ts`、96件)

サーバー側のロジックを、DBに接続せずに確認します。

| 対象 | 主な確認内容 |
|---|---|
| `_shared/availability`(空き枠計算) | 営業時間・スタッフのシフト・休憩・既存予約との重なり、閉店をまたぐ枠、最終受付ちょうどなどの境界値 |
| `_shared/menuSelection`(メニュー選択のルール) | カットは1つまで、パーマ・ツイストは併用可、オプション単独は不可、料金・時間の単純合算、存在しない/非公開メニューの拒否 |
| `_shared/checkout`(会計金額のルール) | 完了の予約にだけ設定できる、「〜」付きを含む予約は完了時に必須、負数・小数・上限超・数値以外の拒否 |
| `_shared/revenue`(売上の集計) | 会計金額の優先、「〜」を含むときの下限フラグ、キャンセル済みの除外 |
| `_shared/validation`・`range`・`rateLimit` | 入力チェック(電話番号など)、時間範囲の解釈、レート制限 |

---

## 6. 確認していないこと(既知のギャップ)
- 確認メールの本文と、実際の送信(手動での確認のみ)。
- 「〜」付きメニューを含む予約を完了にするときの必須入力を、画面から通す確認(単体テストで、ルール自体は確認済み)。
- 管理画面のスケジュール・検索タブの中身の細かい確認(3・4は表示の確認のみ)。
- 各Edge Functionのルーティング・リクエスト検証部分(`*/index.ts`)は、E2Eでしか確認していない。
